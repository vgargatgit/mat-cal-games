export const MISCONCEPTIONS = {
  'decomposition-mismatch': 'One selected assignment changes the expression or skips an operation. Split the expression inside-out, one operation per node.',
  'graph-connection': 'The forward circuit is missing a dependency wire or contains a connection that is not part of the expression.',
  'factor-order': 'Build the route from the derivative nearest the output toward the derivative nearest the input.',
  'accumulation-missing': 'Complete each route separately, then press the addition junction to accumulate the returning contributions.',
  'added-serial': 'Along one dependency path, local sensitivities multiply because each change passes through the next operation.',
  'multiplied-branches': 'These are alternative routes. Calculate each route separately, then add their contributions.',
  'missing-path': 'The selected input reaches the output through another branch that has not been included.',
  'premature-addition': 'Complete the derivative product for each route before combining separate routes.',
  'missing-local': 'This operation lies on the selected path, so its local derivative must appear in the chain product.',
  'wrong-local': 'A wire label must differentiate its immediate output with respect to its immediate input.',
  'value-as-derivative': 'The node value and the node’s local slope are different quantities.',
  'local-as-total': 'That derivative covers one edge. The total derivative must include the remaining path.',
  'incoming-omitted': 'The later computation sends an incoming gradient here. Multiply the local chain by it.',
  'incoming-added': 'The incoming gradient lies on the same backward path, so it multiplies the local derivative.',
  'reversed-order': 'Under numerator layout, begin nearest the final output and move backward toward the input.',
  'matrix-commutative': 'Jacobian multiplication order cannot be swapped.',
  'shape-mismatch': 'Adjacent derivative blocks must have matching inner dimensions.',
  'gradient-overwrite': 'This value has more than one downstream use. Preserve and add every contribution.',
  'gradient-duplicated': 'A gradient splits only when the forward value feeds multiple downstream operations.',
  'zero-wire-deleted': 'The structural path still exists. Its current local derivative blocks the backward signal.',
  'skip-omitted': 'The original input also reaches the output through the identity branch.',
  'product-path-omitted': 'The multiplication node has two active routes. Each contributes one product-rule term.',
  'scalar-vector-confusion': 'Use compatible Jacobian multiplication when nodes are vectors.',
  'wrong-final-shape': 'The derivative has one row per output component and one column per input component.'
};
